{
  "name": "@felippedeiro/backend-appvideos",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "lint": "npx eslint src",
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test:simple": "npx jest --config jest.config.js --runInBand",
    "test:cov": "cross-env NODE_ENV=test npx jest --config jest.config.js --coverage --runInBand --no-cache",
    "test:w:a": "cross-env NODE_ENV=test npx jest --config jest.config.js --watchAll --runInBand",
    "pretest": "cross-env NODE_ENV=test npx sequelize db:migrate",
    "test": "cross-env NODE_ENV=test npx jest --config jest.config.js --runInBand",
    "posttest": "cross-env NODE_ENV=test npx sequelize db:migrate:undo:all",
    "pretest:w": "cross-env NODE_ENV=test npx sequelize db:migrate",
    "test:w": "cross-env NODE_ENV=test npx jest --config jest.config.js --runInBand --watch",
    "posttest:w": "cross-env NODE_ENV=test npx sequelize db:migrate:undo:all"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "cpf-cnpj-validator": "^1.0.3",
    "dotenv": "^10.0.0",
    "elastic-apm-node": "^3.26.0",
    "express": "~4.16.1",
    "helmet": "^4.6.0",
    "ioredis": "^4.28.2",
    "jsonwebtoken": "^8.5.1",
    "kafkajs": "^1.15.0",
    "mysql2": "^2.3.3",
    "pino": "^7.6.1",
    "pino-pretty": "^7.3.0",
    "sequelize": "^6.12.0",
    "uuid": "^8.3.2",
    "yup": "^0.32.11"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.7.0",
    "eslint-config-prettier": "^8.3.0",
    "faker": "^5.5.3",
    "jest": "^27.4.5",
    "nodemon": "^2.0.15",
    "rewire": "^6.0.0",
    "sequelize-cli": "^6.3.0",
    "sqlite3": "^5.0.2",
    "supertest": "^6.1.6"
  }
}
